# Use API Gateway when all services are routed through gateway
VITE_API_BASE_URL=http://localhost:8080

# If you are testing only Auth service directly, temporarily use:
# VITE_API_BASE_URL=http://localhost:5234
